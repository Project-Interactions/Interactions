# Configuration file

general {

    "startup script checks" {
        B:"Disable all checks"=true
        B:"Do not load with missing power tiers"=false
        B:"Do not load with no scripts"=false
        B:"Don't load with ZenScript Syntax errors"=false
        B:"Don't load with ZenScript errors"=true
        B:"Don't load with no reservoirs/power tiers"=true
    }

    "default reservoirs" {
        # This will Register default IP Reservoirs(Oil, Water, Lava), default=true
        B:"Register Reservoirs"=true

        "default pumpjack power tiers" {
            # This will set the capacity of the pumpjack, default=16000
            # Min: 1
            # Max: 2147483647
            I:"Default capacity"=16000

            # This will set the power consumption of the pumpjack, default=1024
            # Min: 1
            # Max: 2147483647
            I:"Default consumption"=1024
        }

    }

}


